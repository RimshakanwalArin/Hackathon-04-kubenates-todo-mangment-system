apiVersion: v1
kind: Service
metadata:
  name: todo-chatbot-backend
  namespace: default
  labels:
    app: todo-chatbot-backend
spec:
  type: ClusterIP
  selector:
    app: todo-chatbot-backend
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
